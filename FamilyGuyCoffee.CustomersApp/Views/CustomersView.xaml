<UserControl x:Class="FamilyGuyCoffee.CustomersApp.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FamilyGuyCoffee.CustomersApp.Views" 
             xmlns:converters="clr-namespace:FamilyGuyCoffee.CustomersApp.Converters"
             xmlns:viewModels="clr-namespace:FamilyGuyCoffee.CustomersApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:CustomersViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NavigationSideToGridColumnConverter x:Key="NavigationSideToGridColumnConvert"></converters:NavigationSideToGridColumnConverter>
    </UserControl.Resources>
    <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid x:Name="CustomerListGrid" Grid.Column="{Binding NavigationSidee, Converter={StaticResource NavigationSideToGridColumnConvert}}"  Background="#777">
            <Grid.RowDefinitions>

                <RowDefinition Height="Auto" />
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button Margin="10" Width="75" Click="ButtonAdd_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/add.png" Height="18" />
                        <TextBlock Text="Add" />
                    </StackPanel>
                </Button>
                <Button Content="Delete" Margin="0 10 10 10" Width="75"></Button>

                <Button Click="ButtonBase_OnClick"  Margin="0 10 10 10">
                    <Image Source="/Images/move.png" Height="18"></Image>
                </Button>
            </StackPanel>
            <ListView x:Name="CustomerListView" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"  DisplayMemberPath="FirstName" Grid.Row="1" Margin="10 0 10 10">
                <!--<ListViewItem>Julia</ListViewItem>
                <ListViewItem>Michael</ListViewItem>
                <ListViewItem>Sarah</ListViewItem>-->
            </ListView>
        </Grid>
        <StackPanel Grid.Column="1" Margin="10">
            <Label>Firstname:</Label>
            <TextBox Text="{Binding SelectedCustomer.FirstName,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Name="FirstnameTextBox"></TextBox>
            <Label>Last Name:</Label>
            <TextBox Text="{Binding SelectedCustomer.LastName,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Name="LastnameTextBox"></TextBox>

            <CheckBox IsChecked="{Binding SelectedCustomer.IsDeveloper, Mode=TwoWay}" Margin="0 10 0 0">
                Is Developer
            </CheckBox>
        </StackPanel>
    </Grid>
</UserControl>
